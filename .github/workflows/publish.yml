name: Deploy To Prodcution
on:
  release:
    types: [published]

jobs:
  only_on_final_release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run your release pipeline
        run: echo "This is a full release!"
